[workspace]
members = [
    "phantom-cli",
    "phantom-ipc",
    "phantom-license",
    "phantom-svc",
    "phantom-tray",
    "phantom-verify",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
